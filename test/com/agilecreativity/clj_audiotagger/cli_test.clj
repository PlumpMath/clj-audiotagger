(ns com.agilecreativity.clj_audiotagger.cli-test
  (:require [clojure.test :refer :all]
            [com.agilecreativity.clj_audiotagger.cli :refer :all]))

(deftest a-test
  (testing "I don't do the whole lot for now"
    (is (= 4 (+ 2 2)))))
